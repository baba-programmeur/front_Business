import { MontantrightPipe } from './montantright.pipe';

describe('MontantrightPipe', () => {
  it('create an instance', () => {
    const pipe = new MontantrightPipe();
    expect(pipe).toBeTruthy();
  });
});
